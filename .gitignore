# dependencies
node_modules/

# next.js build output
.next/
out/

# env files are managed by the platform — never commit them
.env
.env.*
!.env.example

# embedded dev postgres data (disposable — see NOTES.md §1)
.pgdata/
.pgdata.corrupt/
.pgdata-trash-*/

# typescript/build artifacts
*.tsbuildinfo
tsconfig.tsbuildinfo
.deps-done

# misc
.DS_Store
npm-debug.log*

# internal AI-workflow blueprints (workspace-only, not part of the submission)
.drytis/
